# LoadFinder Android

LoadFinder Android client: Kotlin + Jetpack Compose, location-aware freight search, paging, Auto Search, notifications, contractual confirmation gates, and API-first exchange integrations.

The canonical verified source is being synchronized from the local v74 artifact. Cloud builds are performed by GitHub Actions; build success is reported only after an actual Actions run completes successfully.
