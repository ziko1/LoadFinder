# LoadFinder v6

This version moves the project from a UI/backend skeleton toward a real exchange workflow.

Primary completed integration surface: Trans.eu OAuth + proposal details + negotiation + accept + accepted loads + callbacks.

Requires your own approved Trans.eu API application credentials. It does not bypass the exchange, CAPTCHA, authentication, quotas, or platform restrictions.
