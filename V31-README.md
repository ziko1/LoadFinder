# LoadFinder v31

Compile-focused hardening.
- Node engine declared
- TypeScript emit configuration checked against dist/server.js
- relative imports audited
- Android test source set verified
- build input manifest added

Runtime compiler execution is only claimed when dependencies/toolchain actually run.
