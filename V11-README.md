# LoadFinder v11

Consolidated unified exchange engine.

The project now has one conceptual flow:
GPS → all exchange adapters → dedupe → profitability → ranking → driver → BID/ACCEPT → active route → NEXT LOAD.

No API credentials are embedded.
