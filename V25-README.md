# LoadFinder v25
Integration-test foundation for Auto Search, queue/repository behavior and notification outbox.
These are deterministic test doubles; production PostgreSQL/Redis/FCM tests require the runtime dependencies.
