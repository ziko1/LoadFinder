# LoadFinder v27
CI/preflight and pipeline contract tests.
The preflight verifies the accumulated regression test suite is present.
Runtime execution still requires installed Node dependencies and, for real integration tests, Docker/Testcontainers.
