# LoadFinder v23
Auto Search reliability: validated policy bounds and explicit worker state transitions.
Runtime Gradle/TypeScript builds must be executed in an environment with dependencies installed.
